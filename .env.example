PORT=8787
ADMIN_TOKEN=change-me-admin
# EA_TOKEN is unused as of v3.5.2 — the Apex license alone authenticates /api/ea/config and
# /api/ea/event. Safe to leave set or remove; nothing in server.mjs reads it anymore.
SESSION_SECRET=change-me-session-secret
DATA_DIR=./data
